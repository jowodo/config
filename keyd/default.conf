[ids]
*

[main]
# Swap meta and alt key https://github.com/rvaiya/keyd/issues/955
#leftcontrol = layer(control)
leftalt = layer(meta)
leftmeta = layer(alt)
#spacebar
rightmeta = layer(altgr)
rightalt = print


# remape scale to print 
scale = print

# remap key next to left shift to ` and ~
102nd = `

# add delete key
[shift] 
backspace = delete

[control]
backspace = delete
