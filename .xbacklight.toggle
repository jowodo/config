#!/bin/sh
split=3
min=0.1
nmin=${nmin%.*}
max=100
middle=$((max/$split))
nmiddle=${middle%.*}
current=$(xbacklight)
ncurrent=${current%.*}
if [ $ncurrent -eq $max ] 
then 
	xbacklight -set $min
else
	xbacklight -inc $nmiddle
fi
